lexik_jwt_authentication:
  secret_key: '%kernel.project_dir%/config/jwt/private.pem' # Private key path
  public_key: '%kernel.project_dir%/config/jwt/public.pem' # Public key path
  pass_phrase: '%env(JWT_PASSPHRASE)%'  # Must match .env
  token_ttl: 3600  # Token expiration time in seconds